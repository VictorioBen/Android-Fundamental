<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.workspace.githubappconsumer2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <permission
        android:name="com.workspace.githubusertwo.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="com.workspace.githubusertwo.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name=".activity.provider.UserProvider"
            android:authorities="com.workspace.githubusertwo"
            android:readPermission="com.workspace.githubusertwo"
            android:writePermission="com.workspace.githubusertwo"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".activity.activity.SettingActivity"
            android:parentActivityName=".activity.activity.SearchActivity" />

        <receiver
            android:name=".activity.broadcast.AlarmReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".activity.activity.AlarmActivity"
            android:parentActivityName=".activity.activity.SearchActivity" />
        <activity
            android:name=".activity.activity.ListFavoriteActivity"
            android:parentActivityName=".activity.activity.SearchActivity" />
        <activity
            android:name=".activity.activity.DetailUserActivity"
            android:parentActivityName=".activity.activity.SearchActivity" />
        <activity
            android:name=".activity.activity.SearchActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>